jwt.secret=berlian
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3307}/almasgo_core
spring.datasource.username=${MYSQL_USERNAME:root}
spring.datasource.password=${MYSQL_PASSWORD:vexkratm}
logging.level.org.springframework = debug
spring.jpa.properties.hibernate.jdbc.batch_size=100
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.generate_statistics=true


spring.data.elasticsearch.cluster-nodes=${ELASTICSEARCH_HOST:localhost:9201}

spring.mail.host=${MAIL_HOST:smtp.mailtrap.io}
spring.mail.username=${MAIL_USERNAME:dummy}
spring.mail.password=${MAIL_PASSWORD:dummy}
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.smtp.port=${MAIL_PORT:2525}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true



spring.flyway.url = jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3307}/almasgo_core
spring.flyway.user=${MYSQL_USERNAME:root}
spring.flyway.password=${MYSQL_PASSWORD:vexkratm}
spring.flyway.locations=filesystem:./db/migration